/* eslint-disable */

/**
 * Generated by Verge3D Puzzles v.4.1.1
 * Fri, 13 Oct 2023 21:41:38 GMT
 * Prefer not editing this file as your changes may get overridden once Puzzles are saved.
 * Check out https://www.soft8soft.com/docs/manual/en/introduction/Using-JavaScript.html
 * for the information on how to add your own JavaScript to Verge3D apps.
 */

'use strict';

(function() {

// global variables/constants used by puzzles' functions

var LIST_NONE = '<none>';

var _pGlob = {};

_pGlob.objCache = {};
_pGlob.fadeAnnotations = true;
_pGlob.pickedObject = '';
_pGlob.hoveredObject = '';
_pGlob.mediaElements = {};
_pGlob.loadedFile = '';
_pGlob.states = [];
_pGlob.percentage = 0;
_pGlob.openedFile = '';
_pGlob.openedFileMeta = {};
_pGlob.xrSessionAcquired = false;
_pGlob.xrSessionCallbacks = [];
_pGlob.screenCoords = new v3d.Vector2();
_pGlob.intervalTimers = {};
_pGlob.customEvents = new v3d.EventDispatcher();
_pGlob.eventListeners = [];

_pGlob.AXIS_X = new v3d.Vector3(1, 0, 0);
_pGlob.AXIS_Y = new v3d.Vector3(0, 1, 0);
_pGlob.AXIS_Z = new v3d.Vector3(0, 0, 1);
_pGlob.MIN_DRAG_SCALE = 10e-4;
_pGlob.SET_OBJ_ROT_EPS = 1e-8;

_pGlob.vec2Tmp = new v3d.Vector2();
_pGlob.vec2Tmp2 = new v3d.Vector2();
_pGlob.vec3Tmp = new v3d.Vector3();
_pGlob.vec3Tmp2 = new v3d.Vector3();
_pGlob.vec3Tmp3 = new v3d.Vector3();
_pGlob.vec3Tmp4 = new v3d.Vector3();
_pGlob.eulerTmp = new v3d.Euler();
_pGlob.eulerTmp2 = new v3d.Euler();
_pGlob.quatTmp = new v3d.Quaternion();
_pGlob.quatTmp2 = new v3d.Quaternion();
_pGlob.colorTmp = new v3d.Color();
_pGlob.mat4Tmp = new v3d.Matrix4();
_pGlob.planeTmp = new v3d.Plane();
_pGlob.raycasterTmp = new v3d.Raycaster(); // always check visibility

var PL = {};
// backward compatibility
if (v3d[Symbol.toStringTag] !== 'Module') {
    v3d.PL = v3d.puzzles = PL;
}

PL.procedures = PL.procedures || {};




PL.execInitPuzzles = function(options) {
    // always null, should not be available in "init" puzzles
    var appInstance = null;
    // app is more conventional than appInstance (used in exec script and app templates)
    var app = null;

    var _initGlob = {};
    _initGlob.percentage = 0;
    _initGlob.output = {
        initOptions: {
            fadeAnnotations: true,
            useBkgTransp: false,
            preserveDrawBuf: false,
            useCompAssets: false,
            useFullscreen: true,
            useCustomPreloader: false,
            preloaderStartCb: function() {},
            preloaderProgressCb: function() {},
            preloaderEndCb: function() {},
        }
    }

    // provide the container's id to puzzles that need access to the container
    _initGlob.container = options !== undefined && 'container' in options
            ? options.container : "";

    this.procedures["ocultarSubAreas"] = ocultarSubAreas;

    // utility functions envoked by the HTML puzzles
function getElements(ids, isParent) {
    var elems = [];
    if (Array.isArray(ids) && ids[0] != 'CONTAINER' && ids[0] != 'WINDOW' &&
        ids[0] != 'DOCUMENT' && ids[0] != 'BODY' && ids[0] != 'QUERYSELECTOR') {
        for (var i = 0; i < ids.length; i++)
            elems.push(getElement(ids[i], isParent));
    } else {
        elems.push(getElement(ids, isParent));
    }
    return elems;
}

function getElement(id, isParent) {
    var elem;
    if (Array.isArray(id) && id[0] == 'CONTAINER') {
        if (appInstance !== null) {
            elem = appInstance.container;
        } else if (typeof _initGlob !== 'undefined') {
            // if we are on the initialization stage, we still can have access
            // to the container element
            var id = _initGlob.container;
            if (isParent) {
                elem = parent.document.getElementById(id);
            } else {
                elem = document.getElementById(id);
            }
        }
    } else if (Array.isArray(id) && id[0] == 'WINDOW') {
        if (isParent)
            elem = parent;
        else
            elem = window;
    } else if (Array.isArray(id) && id[0] == 'DOCUMENT') {
        if (isParent)
            elem = parent.document;
        else
            elem = document;
    } else if (Array.isArray(id) && id[0] == 'BODY') {
        if (isParent)
            elem = parent.document.body;
        else
            elem = document.body;
    } else if (Array.isArray(id) && id[0] == 'QUERYSELECTOR') {
        if (isParent)
            elem = parent.document.querySelector(id);
        else
            elem = document.querySelector(id);
    } else {
        if (isParent)
            elem = parent.document.getElementById(id);
        else
            elem = document.getElementById(id);
    }
    return elem;
}

// setHTMLElemAttribute puzzle
function setHTMLElemAttribute(attr, value, ids, isParent) {
    var elems = getElements(ids, isParent);
    for (var i = 0; i < elems.length; i++) {
        var elem = elems[i];
        if (!elem) continue;

        if ((attr == 'href' || attr == 'src') && value instanceof Promise) {
            // resolve promise value for url-based attributes
            value.then(function(response) {
                elem[attr] = response;
            });
        } else {
            elem[attr] = value;
        }
    }
}

// setHTMLElemStyle puzzle
function setHTMLElemStyle(prop, value, ids, isParent) {
    var elems = getElements(ids, isParent);
    for (var i = 0; i < elems.length; i++) {
        var elem = elems[i];
        if (!elem || !elem.style)
            continue;
        elem.style[prop] = value;
    }
}

// setTimeout puzzle
function registerSetTimeout(timeout, callback) {
    window.setTimeout(callback, 1000 * timeout);
}

// Describe this function...
function ocultarSubAreas() {
  registerSetTimeout(0.000001, function() {});
}



// initSettings puzzle
_initGlob.output.initOptions.fadeAnnotations = true;
_initGlob.output.initOptions.useBkgTransp = false;
_initGlob.output.initOptions.preserveDrawBuf = false;
_initGlob.output.initOptions.useCompAssets = true;
_initGlob.output.initOptions.useFullscreen = false;


// initPreloader puzzle
_initGlob.output.initOptions.useCustomPreloader = true;
_initGlob.output.initOptions.preloaderStartCb = function() {
    _initGlob.percentage = 0;
    (function() {})();
};
_initGlob.output.initOptions.preloaderProgressCb = function(percentage) {
    _initGlob.percentage = percentage;
    (function() {
  setHTMLElemAttribute('innerHTML', String(Math.round(_initGlob.percentage)) + '%', 'loading_percentage', true);
})();
};
_initGlob.output.initOptions.preloaderEndCb = function() {
    _initGlob.percentage = 100;
    (function() {
  setHTMLElemStyle('display', 'none', 'preloader_screen', true);
  setHTMLElemStyle('display', 'none', 'controles-container', true);
  setHTMLElemStyle('display', 'none', 'go-home', true);
  setHTMLElemStyle('display', 'none', 'go-area', true);
  setHTMLElemStyle('display', 'none', 'cam-torre-captacion-1', true);
  setHTMLElemStyle('display', 'none', 'cam-torre-captacion-2', true);
  setHTMLElemStyle('display', 'none', 'cam-torre-captacion-3', true);
  setHTMLElemStyle('display', 'none', 'cam-carga-hidrociclon', true);
  setHTMLElemStyle('display', 'none', 'cam-nidos-hidrociclones-primera-etapa', true);
  setHTMLElemStyle('display', 'none', 'cam-cajon-descarga-hidrociclones', true);
  setHTMLElemStyle('display', 'none', 'cam-nidos-hidrociclones-segunda-etapa', true);
  setHTMLElemStyle('display', 'none', 'cam-cajones-descarga-segunda-etapa', true);
  setHTMLElemStyle('display', 'none', 'cam-estacion-intermedia', true);
  setHTMLElemStyle('display', 'none', 'logo-img', true);
  setHTMLElemStyle('display', 'flex', 'btn_empezar_recorrido_5', true);
  setHTMLElemStyle('display', 'none', 'btn_empezar_recorrido_1', true);
  setHTMLElemStyle('display', 'none', 'btn_empezar_recorrido_2', true);
  setHTMLElemStyle('display', 'none', 'btn_empezar_recorrido_3', true);
  setHTMLElemStyle('display', 'none', 'btn_empezar_recorrido_4', true);
  setHTMLElemStyle('display', 'none', 'btn_empezar_recorrido_6', true);
  setHTMLElemStyle('display', 'none', 'btn_empezar_recorrido_0', true);
})();
};

    return _initGlob.output;
}

PL.init = function(appInstance, initOptions) {

// app is more conventional than appInstance (used in exec script and app templates)
var app = appInstance;

initOptions = initOptions || {};

if ('fadeAnnotations' in initOptions) {
    _pGlob.fadeAnnotations = initOptions.fadeAnnotations;
}

this.procedures["ocultar-controles-botones"] = ocultar_controles_botones;
this.procedures["mostrar-controles-botones"] = mostrar_controles_botones;
this.procedures["ocultarSubAreas"] = ocultarSubAreas;
this.procedures["ocultar-objetos-riesgos-all"] = ocultar_objetos_riesgos_all;
this.procedures["mostrarSubAreas"] = mostrarSubAreas;
this.procedures["goBack"] = goBack;
this.procedures["deshabilitar-boton-retornar"] = deshabilitar_boton_retornar;
this.procedures["habilitar-boton-retornar"] = habilitar_boton_retornar;
this.procedures["setUserPosition"] = setUserPosition;
this.procedures["setInformacion"] = setInformacion;

var PROC = {
    "ocultar-controles-botones": ocultar_controles_botones,
    "mostrar-controles-botones": mostrar_controles_botones,
    "ocultarSubAreas": ocultarSubAreas,
    "ocultar-objetos-riesgos-all": ocultar_objetos_riesgos_all,
    "mostrarSubAreas": mostrarSubAreas,
    "goBack": goBack,
    "deshabilitar-boton-retornar": deshabilitar_boton_retornar,
    "habilitar-boton-retornar": habilitar_boton_retornar,
    "setUserPosition": setUserPosition,
    "setInformacion": setInformacion,
};

var VARS = Object.defineProperties({}, {
    'operadores-visibles': { get: function() { return operadores_visibles; }, set: function(val) { operadores_visibles = val; } },
    'active-cam': { get: function() { return active_cam; }, set: function(val) { active_cam = val; } },
    'cam-id': { get: function() { return cam_id; }, set: function(val) { cam_id = val; } },
    'operador': { get: function() { return operador; }, set: function(val) { operador = val; } },
    'objeto-riesgo-mostrar': { get: function() { return objeto_riesgo_mostrar; }, set: function(val) { objeto_riesgo_mostrar = val; } },
    'information_id': { get: function() { return information_id; }, set: function(val) { information_id = val; } },
});

var operadores_visibles, active_cam, cam_id, operador, objeto_riesgo_mostrar, information_id;

// featureAvailable puzzle
function featureAvailable(feature) {

    var userAgent = window.navigator.userAgent;
    var platform = window.navigator.platform;

    switch (feature) {
    case 'LINUX':
        return /Linux/.test(platform);
    case 'WINDOWS':
        return ['Win32', 'Win64', 'Windows', 'WinCE'].indexOf(platform) !== -1;
    case 'MACOS':
        return (['Macintosh', 'MacIntel', 'MacPPC', 'Mac68K'].indexOf(platform) !== -1 && !v3d.Detector.checkIOS());
    case 'IOS':
        return v3d.Detector.checkIOS();
    case 'ANDROID':
        return /Android/i.test(userAgent);
    case 'MOBILE':
        return (/Android|webOS|BlackBerry/i.test(userAgent) || v3d.Detector.checkIOS());

    case 'CHROME':
        // Chromium based
        return (!!window.chrome && !/Edge/.test(navigator.userAgent));
    case 'FIREFOX':
        return /Firefox/.test(navigator.userAgent);
    case 'IE':
        return /Trident/.test(navigator.userAgent);
    case 'EDGE':
        return /Edge/.test(navigator.userAgent);
    case 'SAFARI':
        return (/Safari/.test(navigator.userAgent) && !/Chrome/.test(navigator.userAgent));

    case 'TOUCH':
        return !!(('ontouchstart' in window) || window.DocumentTouch && document instanceof DocumentTouch);
    case 'RETINA':
        return window.devicePixelRatio >= 2;
    case 'HDR':
        return appInstance.useHDR;
    case 'WEBAUDIO':
        return v3d.Detector.checkWebAudio();
    case 'WEBGL':
        var canvas = document.createElement('canvas');
        var gl = canvas.getContext('webgl')
        return !!gl;
    case 'WEBGL2':
        var canvas = document.createElement('canvas');
        var gl = canvas.getContext('webgl2')
        return !!gl;
    case 'WOOCOMMERCE':
        var woo_fun = window.parent.v3d_woo_get_product_info || window.parent.parent.v3d_woo_get_product_info;
        return !!woo_fun;
    case 'DO_NOT_TRACK':
        if (navigator.doNotTrack == '1' || window.doNotTrack == '1')
            return true;
        else
            return false;
    default:
        return false;
    }

}

// utility functions envoked by the HTML puzzles
function getElements(ids, isParent) {
    var elems = [];
    if (Array.isArray(ids) && ids[0] != 'CONTAINER' && ids[0] != 'WINDOW' &&
        ids[0] != 'DOCUMENT' && ids[0] != 'BODY' && ids[0] != 'QUERYSELECTOR') {
        for (var i = 0; i < ids.length; i++)
            elems.push(getElement(ids[i], isParent));
    } else {
        elems.push(getElement(ids, isParent));
    }
    return elems;
}

function getElement(id, isParent) {
    var elem;
    if (Array.isArray(id) && id[0] == 'CONTAINER') {
        if (appInstance !== null) {
            elem = appInstance.container;
        } else if (typeof _initGlob !== 'undefined') {
            // if we are on the initialization stage, we still can have access
            // to the container element
            var id = _initGlob.container;
            if (isParent) {
                elem = parent.document.getElementById(id);
            } else {
                elem = document.getElementById(id);
            }
        }
    } else if (Array.isArray(id) && id[0] == 'WINDOW') {
        if (isParent)
            elem = parent;
        else
            elem = window;
    } else if (Array.isArray(id) && id[0] == 'DOCUMENT') {
        if (isParent)
            elem = parent.document;
        else
            elem = document;
    } else if (Array.isArray(id) && id[0] == 'BODY') {
        if (isParent)
            elem = parent.document.body;
        else
            elem = document.body;
    } else if (Array.isArray(id) && id[0] == 'QUERYSELECTOR') {
        if (isParent)
            elem = parent.document.querySelector(id);
        else
            elem = document.querySelector(id);
    } else {
        if (isParent)
            elem = parent.document.getElementById(id);
        else
            elem = document.getElementById(id);
    }
    return elem;
}

// setHTMLElemStyle puzzle
function setHTMLElemStyle(prop, value, ids, isParent) {
    var elems = getElements(ids, isParent);
    for (var i = 0; i < elems.length; i++) {
        var elem = elems[i];
        if (!elem || !elem.style)
            continue;
        elem.style[prop] = value;
    }
}

function _checkListenersSame(target0, type0, listener0, optionsOrUseCapture0,
        target1, type1, listener1, optionsOrUseCapture1) {
    const capture0 = Boolean(optionsOrUseCapture0 instanceof Object
            ? optionsOrUseCapture0.capture : optionsOrUseCapture0);
    const capture1 = Boolean(optionsOrUseCapture1 instanceof Object
            ? optionsOrUseCapture1.capture : optionsOrUseCapture1);
    return target0 === target1 && type0 === type1 && listener0 === listener1
            && capture0 === capture1;
}

/**
 * Add the specified event listener to the specified target. This function also
 * stores listener data for easier disposing.
 */
function bindListener(target, type, listener, optionsOrUseCapture) {
    const alreadyExists = _pGlob.eventListeners.some(elem => {
        return _checkListenersSame(elem.target, elem.type, elem.listener,
                elem.optionsOrUseCapture, target, type, listener,
                optionsOrUseCapture);
    });

    if (!alreadyExists) {
        target.addEventListener(type, listener, optionsOrUseCapture);
        _pGlob.eventListeners.push({ target, type, listener, optionsOrUseCapture });
    }
}

// eventHTMLElem puzzle
function eventHTMLElem(eventType, ids, isParent, callback) {
    var elems = getElements(ids, isParent);
    for (var i = 0; i < elems.length; i++) {
        var elem = elems[i];
        if (!elem)
            continue;

        bindListener(elem, eventType, callback);
    }
}

// getHTMLElemAttribute puzzle
function getHTMLElemAttribute(attr, id, isParent) {
    var elem = getElement(id, isParent);
    return elem ? elem[attr]: '';
}

// utility function envoked by almost all V3D-specific puzzles
// filter off some non-mesh types
function notIgnoredObj(obj) {
    return obj.type !== 'AmbientLight' &&
           obj.name !== '' &&
           !(obj.isMesh && obj.isMaterialGeneratedMesh) &&
           !obj.isAuxClippingMesh;
}


// utility function envoked by almost all V3D-specific puzzles
// find first occurence of the object by its name
function getObjectByName(objName) {
    var objFound;
    var runTime = _pGlob !== undefined;
    objFound = runTime ? _pGlob.objCache[objName] : null;

    if (objFound && objFound.name === objName)
        return objFound;

    if (appInstance.scene) {
        appInstance.scene.traverse(function(obj) {
            if (!objFound && notIgnoredObj(obj) && (obj.name == objName)) {
                objFound = obj;
                if (runTime) {
                    _pGlob.objCache[objName] = objFound;
                }
            }
        });
    }
    return objFound;
}


// utility function envoked by almost all V3D-specific puzzles
// retrieve all objects on the scene
function getAllObjectNames() {
    var objNameList = [];
    appInstance.scene.traverse(function(obj) {
        if (notIgnoredObj(obj))
            objNameList.push(obj.name)
    });
    return objNameList;
}


// utility function envoked by almost all V3D-specific puzzles
// retrieve all objects which belong to the group
function getObjectNamesByGroupName(targetGroupName) {
    var objNameList = [];
    appInstance.scene.traverse(function(obj){
        if (notIgnoredObj(obj)) {
            var groupNames = obj.groupNames;
            if (!groupNames)
                return;
            for (var i = 0; i < groupNames.length; i++) {
                var groupName = groupNames[i];
                if (groupName == targetGroupName) {
                    objNameList.push(obj.name);
                }
            }
        }
    });
    return objNameList;
}


// utility function envoked by almost all V3D-specific puzzles
// process object input, which can be either single obj or array of objects, or a group
function retrieveObjectNames(objNames) {
    var acc = [];
    retrieveObjectNamesAcc(objNames, acc);
    return acc.filter(function(name) {
        return name;
    });
}

function retrieveObjectNamesAcc(currObjNames, acc) {
    if (typeof currObjNames == "string") {
        acc.push(currObjNames);
    } else if (Array.isArray(currObjNames) && currObjNames[0] == "GROUP") {
        var newObj = getObjectNamesByGroupName(currObjNames[1]);
        for (var i = 0; i < newObj.length; i++)
            acc.push(newObj[i]);
    } else if (Array.isArray(currObjNames) && currObjNames[0] == "ALL_OBJECTS") {
        var newObj = getAllObjectNames();
        for (var i = 0; i < newObj.length; i++)
            acc.push(newObj[i]);
    } else if (Array.isArray(currObjNames)) {
        for (var i = 0; i < currObjNames.length; i++)
            retrieveObjectNamesAcc(currObjNames[i], acc);
    }
}

// utility function used by the whenClicked, whenHovered and whenDraggedOver puzzles
function initObjectPicking(callback, eventType, mouseDownUseTouchStart, mouseButtons) {

    var elem = appInstance.renderer.domElement;
    bindListener(elem, eventType, pickListener);

    if (eventType == 'mousedown') {

        var touchEventName = mouseDownUseTouchStart ? 'touchstart' : 'touchend';
        bindListener(elem, touchEventName, pickListener);

    } else if (eventType == 'dblclick') {

        var prevTapTime = 0;

        function doubleTapCallback(event) {

            var now = new Date().getTime();
            var timesince = now - prevTapTime;

            if (timesince < 600 && timesince > 0) {

                pickListener(event);
                prevTapTime = 0;
                return;

            }

            prevTapTime = new Date().getTime();
        }

        var touchEventName = mouseDownUseTouchStart ? 'touchstart' : 'touchend';
        bindListener(elem, touchEventName, doubleTapCallback);
    }

    var raycaster = new v3d.Raycaster();

    function pickListener(event) {

        // to handle unload in loadScene puzzle
        if (!appInstance.getCamera())
            return;

        event.preventDefault();

        var xNorm = 0, yNorm = 0;
        if (event instanceof MouseEvent) {
            if (mouseButtons && mouseButtons.indexOf(event.button) == -1)
                return;
            xNorm = event.offsetX / elem.clientWidth;
            yNorm = event.offsetY / elem.clientHeight;
        } else if (event instanceof TouchEvent) {
            var rect = elem.getBoundingClientRect();
            xNorm = (event.changedTouches[0].clientX - rect.left) / rect.width;
            yNorm = (event.changedTouches[0].clientY - rect.top) / rect.height;
        }

        _pGlob.screenCoords.x = xNorm * 2 - 1;
        _pGlob.screenCoords.y = -yNorm * 2 + 1;
        raycaster.setFromCamera(_pGlob.screenCoords, appInstance.getCamera(true));
        var objList = [];
        appInstance.scene.traverse(function(obj){objList.push(obj);});
        var intersects = raycaster.intersectObjects(objList, false);
        callback(intersects, event);
    }
}

function objectsIncludeObj(objNames, testedObjName) {
    if (!testedObjName) return false;

    for (var i = 0; i < objNames.length; i++) {
        if (testedObjName == objNames[i]) {
            return true;
        } else {
            // also check children which are auto-generated for multi-material objects
            var obj = getObjectByName(objNames[i]);
            if (obj && obj.type == "Group") {
                for (var j = 0; j < obj.children.length; j++) {
                    if (testedObjName == obj.children[j].name) {
                        return true;
                    }
                }
            }
        }
    }
    return false;
}

// utility function used by the whenClicked, whenHovered, whenDraggedOver, and raycast puzzles
function getPickedObjectName(obj) {
    // auto-generated from a multi-material object, use parent name instead
    if (obj.isMesh && obj.isMaterialGeneratedMesh && obj.parent) {
        return obj.parent.name;
    } else {
        return obj.name;
    }
}

// whenClicked puzzle
function registerOnClick(objSelector, xRay, doubleClick, mouseButtons, cbDo, cbIfMissedDo) {

    // for AR/VR
    _pGlob.objClickInfo = _pGlob.objClickInfo || [];

    _pGlob.objClickInfo.push({
        objSelector: objSelector,
        callbacks: [cbDo, cbIfMissedDo]
    });

    initObjectPicking(function(intersects, event) {

        var isPicked = false;

        var maxIntersects = xRay ? intersects.length : Math.min(1, intersects.length);

        for (var i = 0; i < maxIntersects; i++) {
            var obj = intersects[i].object;
            var objName = getPickedObjectName(obj);
            var objNames = retrieveObjectNames(objSelector);

            if (objectsIncludeObj(objNames, objName)) {
                // save the object for the pickedObject block
                _pGlob.pickedObject = objName;
                isPicked = true;
                cbDo(event);
            }
        }

        if (!isPicked) {
            _pGlob.pickedObject = '';
            cbIfMissedDo(event);
        }

    }, doubleClick ? 'dblclick' : 'mousedown', false, mouseButtons);
}

// Describe this function...
function ocultar_controles_botones() {
  setHTMLElemStyle('display', 'none', 'controles-container', true);
  setHTMLElemStyle('display', 'none', 'go-home', true);
  setHTMLElemStyle('display', 'none', 'go-area', true);
}

function MediaHTML5(isVideo) {
    this.source = null;
}

Object.assign(MediaHTML5.prototype, {

    load: function(url, isVideo) {
        if (isVideo) {
            this.source = document.createElement('video');
            this.source.playsInline = true;
            this.source.preload = 'auto';
            this.source.autoload = true;
            this.source.crossOrigin = 'anonymous';
        } else {
            this.source = document.createElement('audio');
        }

        this.source.src = url;
        return this;
    },

    play: function() {
        this.source.play();
    },

    pause: function() {
        this.source.pause();
    },

    stop: function() {
        this.source.pause();
        this.source.currentTime = 0;
    },

    rewind: function() {
        this.source.currentTime = 0;
    },

    setPlaybackTime: function(time) {
        this.source.currentTime = time
    },

    getPlaybackTime: function() {
        return this.source.currentTime;
    },

    getDuration: function() {
        return this.source.duration;
    },

    setPlaybackRate: function(rate) {
        this.source.playbackRate = rate;
    },

    isPlaying: function() {
        return this.source.duration > 0 && !this.source.paused;
    },

    setLoop: function(looped) {
        this.source.loop = looped;
    },

    setVolume: function(volume) {
        this.source.volume = volume;
    },

    setMuted: function(muted) {
        this.source.muted = muted;
    },

});

// loadMedia puzzle
function loadMedia_HTML5(url) {

    var elems = _pGlob.mediaElements;
    if (!(url in elems)) {
        elems[url] = new MediaHTML5().load(url);
    }
    return elems[url];
}

// soundVideoMute puzzle
function soundVideoMute(mediaElem) {
    const mediaElems = (Array.isArray(mediaElem) ? mediaElem : [mediaElem]).filter(elem => elem);
    mediaElems.forEach(mediaElem => {
        mediaElem.setMuted(true);
    });
}

// soundVideoUnmute puzzle
function soundVideoUnmute(mediaElem) {
    const mediaElems = (Array.isArray(mediaElem) ? mediaElem : [mediaElem]).filter(elem => elem);
    mediaElems.forEach(mediaElem => {
        mediaElem.setMuted(false);
    });
}

// playSound puzzle
function playSound(mediaElem, loop) {
    const mediaElems = (Array.isArray(mediaElem) ? mediaElem : [mediaElem]).filter(elem => elem);
    mediaElems.forEach(mediaElem => {
        mediaElem.setLoop(loop);
        mediaElem.play();
    });
}

/**
 * Retrieve coordinate system from the loaded scene
 */
function getCoordSystem() {
    var scene = appInstance.scene;

    if (scene && 'coordSystem' in scene.userData) {
        return scene.userData.coordSystem;
    }

    return 'Y_UP_RIGHT';
}


/**
 * Transform coordinates from one space to another
 * Can be used with Vector3 or Euler.
 */
function coordsTransform(coords, from, to, noSignChange) {

    if (from == to)
        return coords;

    var y = coords.y, z = coords.z;

    if (from == 'Z_UP_RIGHT' && to == 'Y_UP_RIGHT') {
        coords.y = z;
        coords.z = noSignChange ? y : -y;
    } else if (from == 'Y_UP_RIGHT' && to == 'Z_UP_RIGHT') {
        coords.y = noSignChange ? z : -z;
        coords.z = y;
    } else {
        console.error('coordsTransform: Unsupported coordinate space');
    }

    return coords;
}


/**
 * Verge3D euler rotation to Blender/Max shortest.
 * 1) Convert from intrinsic rotation (v3d) to extrinsic XYZ (Blender/Max default
 *    order) via reversion: XYZ -> ZYX
 * 2) swizzle ZYX->YZX
 * 3) choose the shortest rotation to resemble Blender's behavior
 */
const eulerV3DToBlenderShortest = function() {

    const eulerTmp = new v3d.Euler();
    const eulerTmp2 = new v3d.Euler();
    const vec3Tmp = new v3d.Vector3();

    return function(euler, dest) {

        const eulerBlender = eulerTmp.copy(euler).reorder('YZX');
        const eulerBlenderAlt = eulerTmp2.copy(eulerBlender).makeAlternative();

        const len = vec3Tmp.setFromEuler(eulerBlender).lengthSq();
        const lenAlt = vec3Tmp.setFromEuler(eulerBlenderAlt).lengthSq();

        dest.copy(len < lenAlt ? eulerBlender : eulerBlenderAlt);
        return coordsTransform(dest, 'Y_UP_RIGHT', 'Z_UP_RIGHT');
    }

}();

// tweenCamera puzzle
function tweenCamera(posOrObj, targetOrObj, duration, doSlot, movementType) {
    var camera = appInstance.getCamera();

    if (Array.isArray(posOrObj)) {
        var worldPos = _pGlob.vec3Tmp.fromArray(posOrObj);
        worldPos = coordsTransform(worldPos, getCoordSystem(), 'Y_UP_RIGHT');
    } else if (posOrObj) {
        var posObj = getObjectByName(posOrObj);
        if (!posObj) return;
        var worldPos = posObj.getWorldPosition(_pGlob.vec3Tmp);
    } else {
        // empty input means: don't change the position
        var worldPos = camera.getWorldPosition(_pGlob.vec3Tmp);
    }

    if (Array.isArray(targetOrObj)) {
        var worldTarget = _pGlob.vec3Tmp2.fromArray(targetOrObj);
        worldTarget = coordsTransform(worldTarget, getCoordSystem(), 'Y_UP_RIGHT');
    } else {
        var targObj = getObjectByName(targetOrObj);
        if (!targObj) return;
        var worldTarget = targObj.getWorldPosition(_pGlob.vec3Tmp2);
    }

    duration = Math.max(0, duration);

    if (appInstance.controls && appInstance.controls.tween) {
        // orbit and flying cameras
        if (!appInstance.controls.inTween) {
            appInstance.controls.tween(worldPos, worldTarget, duration, doSlot,
                    movementType);
        }
    } else {
        // TODO: static camera, just position it for now
        if (camera.parent) {
            camera.parent.worldToLocal(worldPos);
        }
        camera.position.copy(worldPos);
        camera.lookAt(worldTarget);
        doSlot();
    }
}

// getEntityByName puzzle
function getEntityByName(name, getWhat) {
    switch (getWhat) {
        case 'OBJECT':
        case 'ANIMATION':
        case 'MATERIAL':
            return name;
        case 'GROUP':
            return ['GROUP', name];
    }
}

// setTimeout puzzle
function registerSetTimeout(timeout, callback) {
    window.setTimeout(callback, 1000 * timeout);
}

// Describe this function...
function mostrar_controles_botones() {
  setHTMLElemStyle('display', 'flex', 'controles-container', true);
  setHTMLElemStyle('display', 'flex', 'go-home', true);
  setHTMLElemStyle('display', 'flex', 'go-area', true);
}

// Describe this function...
function ocultarSubAreas() {
  registerSetTimeout(0.000001, function() {
    setHTMLElemStyle('display', 'none', 'controles-container', true);
    setHTMLElemStyle('display', 'none', 'go-home', true);
    setHTMLElemStyle('display', 'none', 'go-area', true);
    setHTMLElemStyle('display', 'none', 'cam-torre-captacion-1', true);
    setHTMLElemStyle('display', 'none', 'cam-torre-captacion-2', true);
    setHTMLElemStyle('display', 'none', 'cam-torre-captacion-3', true);
    setHTMLElemStyle('display', 'none', 'cam-carga-hidrociclon', true);
    setHTMLElemStyle('display', 'none', 'cam-nidos-hidrociclones-primera-etapa', true);
    setHTMLElemStyle('display', 'none', 'cam-cajon-descarga-hidrociclones', true);
    setHTMLElemStyle('display', 'none', 'cam-nidos-hidrociclones-segunda-etapa', true);
    setHTMLElemStyle('display', 'none', 'cam-cajones-descarga-segunda-etapa', true);
    setHTMLElemStyle('display', 'none', 'cam-estacion-intermedia', true);
  });
}

// setActiveCamera puzzle
function setActiveCamera(camName) {
    var camera = getObjectByName(camName);
    if (!camera || !camera.isCamera || appInstance.getCamera() == camera)
        return;
    appInstance.setCamera(camera);
}

// show and hide puzzles
function changeVis(objSelector, bool) {
    var objNames = retrieveObjectNames(objSelector);

    for (var i = 0; i < objNames.length; i++) {
        var objName = objNames[i]
        if (!objName)
            continue;
        var obj = getObjectByName(objName);
        if (!obj)
            continue;
        obj.visible = bool;
        obj.resolveMultiMaterial().forEach(function(objR) {
            objR.visible = bool;
        });
    }
}

// Describe this function...
function ocultar_objetos_riesgos_all() {
  changeVis('Icono001.004', false);
  changeVis('Icono001.001', false);
  changeVis('Icono001.002', false);
  changeVis('Icono001.003', false);
  changeVis('Icono001.005', false);
  changeVis('Icono001.006', false);
  changeVis('Icono001.007', false);
  changeVis('Icono001.008', false);
  changeVis('Icono001.009', false);
}

// whenHovered puzzle
initObjectPicking(function(intersects, event) {

    var prevHovered = _pGlob.hoveredObject;
    var currHovered = '';

    // the event might happen before hover registration
    _pGlob.objHoverInfo = _pGlob.objHoverInfo || [];

    // search for closest hovered object

    var lastIntersectIndex = Infinity;
    _pGlob.objHoverInfo.forEach(function(el) {
        var maxIntersects = el.xRay ? intersects.length : Math.min(1, intersects.length);

        for (var i = 0; i < maxIntersects; i++) {
            var obj = intersects[i].object;
            var objName = getPickedObjectName(obj);

            if (objectsIncludeObj(retrieveObjectNames(el.objSelector), objName) && i <= lastIntersectIndex) {
                currHovered = objName;
                lastIntersectIndex = i;
            }
        }
    });

    if (prevHovered == currHovered) return;

    // first - all "out" callbacks, then - all "over"
    _pGlob.objHoverInfo.forEach(function(el) {
        if (objectsIncludeObj(retrieveObjectNames(el.objSelector), prevHovered)) {
            // ensure the correct value of the hoveredObject block
            _pGlob.hoveredObject = prevHovered;
            el.callbacks[1](event);
        }
    });

    _pGlob.objHoverInfo.forEach(function(el) {
        if (objectsIncludeObj(retrieveObjectNames(el.objSelector), currHovered)) {
            // ensure the correct value of the hoveredObject block
            _pGlob.hoveredObject = currHovered;
            el.callbacks[0](event);
        }
    });

    _pGlob.hoveredObject = currHovered;
}, 'mousemove', false);

// whenHovered puzzle
function registerOnHover(objSelector, xRay, cbOver, cbOut) {

    _pGlob.objHoverInfo = _pGlob.objHoverInfo || [];

    _pGlob.objHoverInfo.push({
        objSelector: objSelector,
        callbacks: [cbOver, cbOut],
        xRay: xRay
    });
}

// Describe this function...
function mostrarSubAreas() {
  setHTMLElemStyle('display', 'flex', 'controles-container', true);
  setHTMLElemStyle('display', 'flex', 'go-home', true);
  setHTMLElemStyle('display', 'flex', 'go-area', true);
  setHTMLElemStyle('display', 'flex', 'cam-torre-captacion-1', true);
  setHTMLElemStyle('display', 'flex', 'cam-torre-captacion-2', true);
  setHTMLElemStyle('display', 'flex', 'cam-torre-captacion-3', true);
  setHTMLElemStyle('display', 'flex', 'cam-carga-hidrociclon', true);
  setHTMLElemStyle('display', 'flex', 'cam-nidos-hidrociclones-primera-etapa', true);
  setHTMLElemStyle('display', 'flex', 'cam-cajon-descarga-hidrociclones', true);
  setHTMLElemStyle('display', 'flex', 'cam-nidos-hidrociclones-segunda-etapa', true);
  setHTMLElemStyle('display', 'flex', 'cam-cajones-descarga-segunda-etapa', true);
  setHTMLElemStyle('display', 'flex', 'cam-estacion-intermedia', true);
}

// Describe this function...
function goBack() {
  tweenCamera('Camara006', 'CamaraDir006', 2, function() {
    deshabilitar_boton_retornar();
  }, 0);
}

// setHTMLElemAttribute puzzle
function setHTMLElemAttribute(attr, value, ids, isParent) {
    var elems = getElements(ids, isParent);
    for (var i = 0; i < elems.length; i++) {
        var elem = elems[i];
        if (!elem) continue;

        if ((attr == 'href' || attr == 'src') && value instanceof Promise) {
            // resolve promise value for url-based attributes
            value.then(function(response) {
                elem[attr] = response;
            });
        } else {
            elem[attr] = value;
        }
    }
}

// Describe this function...
function deshabilitar_boton_retornar() {
  setHTMLElemAttribute('style', 'pointer-events:none;opacity:0.5;', 'go-area', true);
}

// Describe this function...
function habilitar_boton_retornar() {
  setHTMLElemAttribute('style', 'pointer-events:initial;opacity:1;', 'go-area', true);
}

// callJSFunction puzzle
function getJSFunction(funcName) {
    var jsFunc = appInstance.ExternalInterface[funcName];
    if (typeof jsFunc == "function")
        return jsFunc;
    else
        return function() {};
}

// Describe this function...
function setUserPosition(active_cam, cam_id, operador, objeto_riesgo_mostrar) {
  tweenCamera(operador, active_cam, 2, function() {
    if (cam_id != false) {
      setHTMLElemAttribute('value', cam_id, 'estado_equipo', true);
      getJSFunction('emitirEstadoEquipo')();
    }
    setActiveCamera(active_cam);
    changeVis(operador, false);
    changeVis(objeto_riesgo_mostrar, true);
    habilitar_boton_retornar();
  }, 1);
}

// Describe this function...
function setInformacion(information_id) {
  setHTMLElemAttribute('value', information_id, 'information_id', true);
  setHTMLElemAttribute('value', information_id, 'estado_animacion', true);
  getJSFunction('emitirEstadoAnimacion')();
}


if (featureAvailable('MOBILE') == true) {
  setHTMLElemStyle('display', 'flex', 'instruccionesmovil-button', false);
  eventHTMLElem('click', 'instruccionesmovil-button', false, function(event) {
    setHTMLElemStyle('display', 'flex', 'intrucciones-movil', false);
  });
  if (getHTMLElemAttribute('offsetHeight', 'v3d-container', false) / getHTMLElemAttribute('offsetWidth', 'v3d-container', false) < 1) {
    setHTMLElemStyle('display', 'none', 'horizontalContainer', true);
  } else if (getHTMLElemAttribute('offsetHeight', 'v3d-container', false) / getHTMLElemAttribute('offsetWidth', 'v3d-container', false) >= 1) {
    setHTMLElemStyle('display', 'flex', 'horizontalContainer', true);
  }
  registerOnClick('Icono001.004', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_1_mobile', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_1_cerrar_mobile', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_1_mobile', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.001', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_2_mobile', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_2_cerrar_mobile', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_2_mobile', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.002', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_3_mobile', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_3_cerrar_mobile', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_3_mobile', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.003', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_4_mobile', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_4_cerrar_mobile', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_4_mobile', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.005', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_5_mobile', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_5_cerrar_mobile', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_5_mobile', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.006', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_6_mobile', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_6_cerrar_mobile', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_6_mobile', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.007', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_7_mobile', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_7_cerrar_mobile', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_7_mobile', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.008', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_8_mobile', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_8_cerrar_mobile', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_8_mobile', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.009', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_9_mobile', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_9_cerrar_mobile', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_9_mobile', false);
    mostrar_controles_botones();
  });
} else {
  setHTMLElemStyle('display', 'flex', 'instrucciones-button', false);
  eventHTMLElem('click', 'instrucciones-button', false, function(event) {
    setHTMLElemStyle('display', 'flex', 'intrucciones-container', false);
  });
  setHTMLElemStyle('display', 'none', 'horizontalContainer', true);
  registerOnClick('Icono001.004', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_1', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_1_cerrar', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_1', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.001', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_2', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_2_cerrar', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_2', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.002', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_3', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_3_cerrar', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_3', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.003', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_4', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_4_cerrar', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_4', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.005', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_5', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_5_cerrar', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_5', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.006', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_6', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_6_cerrar', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_6', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.007', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_7', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_7_cerrar', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_7', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.008', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_8', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_8_cerrar', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_8', false);
    mostrar_controles_botones();
  });
  registerOnClick('Icono001.009', false, false, [0,1,2], function() {
    setHTMLElemStyle('display', 'flex', 'riesgo_9', false);
    ocultar_controles_botones();
  }, function() {});
  eventHTMLElem('click', 'riesgo_9_cerrar', false, function(event) {
    setHTMLElemStyle('display', 'none', 'riesgo_9', false);
    mostrar_controles_botones();
  });
}

eventHTMLElem('click', 'btn_mute_audio', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_mute_audio', false);
  setHTMLElemStyle('display', 'flex', 'btn_unmute_audio', false);
  soundVideoMute(loadMedia_HTML5('../audios/area-4000-parte-a.mp3'));
  soundVideoMute(loadMedia_HTML5('../audios/area-4000-parte-b.mp3'));
  soundVideoMute(loadMedia_HTML5('../audios/area-4000-parte-c.mp3'));
  soundVideoMute(loadMedia_HTML5('../audios/area-4000-parte-d.mp3'));
});
eventHTMLElem('click', 'btn_unmute_audio', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_unmute_audio', false);
  setHTMLElemStyle('display', 'flex', 'btn_mute_audio', false);
  soundVideoUnmute(loadMedia_HTML5('../audios/area-4000-parte-a.mp3'));
  soundVideoUnmute(loadMedia_HTML5('../audios/area-4000-parte-b.mp3'));
  soundVideoUnmute(loadMedia_HTML5('../audios/area-4000-parte-c.mp3'));
  soundVideoUnmute(loadMedia_HTML5('../audios/area-4000-parte-d.mp3'));
});
eventHTMLElem('click', 'btn_empezar_recorrido_5', true, function(event) {
  soundVideoUnmute(loadMedia_HTML5('../audios/area-4000-parte-a.mp3'));
  soundVideoUnmute(loadMedia_HTML5('../audios/area-4000-parte-b.mp3'));
  soundVideoUnmute(loadMedia_HTML5('../audios/area-4000-parte-c.mp3'));
  soundVideoUnmute(loadMedia_HTML5('../audios/area-4000-parte-d.mp3'));
  setHTMLElemStyle('display', 'none', 'btn_empezar_recorrido_5', true);
  setHTMLElemStyle('display', 'flex', 'btn_mute_audio', false);
  setHTMLElemStyle('display', 'flex', 'invisibleContainer', false);
  registerSetTimeout(1.5, function() {
    playSound(loadMedia_HTML5('../audios/area-4000-parte-a.mp3'), false);
    registerSetTimeout(2, function() {
      playSound(loadMedia_HTML5('../audios/area-4000-parte-b.mp3'), false);
      tweenCamera('Camara006', 'CamaraDir006', 2, function() {}, 0);
      registerSetTimeout(2, function() {
        tweenCamera(getEntityByName('Camara005', 'OBJECT'), getEntityByName('CamaraDir005', 'OBJECT'), 2, function() {}, 0);
        registerSetTimeout(2.5, function() {

          Function('app', 'v3d', 'puzzles', 'VARS', 'PROC', ' document.querySelector("#fadeContainer").classList.toggle("fade",false)')(appInstance, v3d, PL, VARS, PROC);

          setHTMLElemStyle('display', 'flex', 'fadeContainer', false);
          registerSetTimeout(1, function() {

            Function('app', 'v3d', 'puzzles', 'VARS', 'PROC', '   document.querySelector("#fadeContainer").classList.toggle("fade")')(appInstance, v3d, PL, VARS, PROC);

            registerSetTimeout(1, function() {
              tweenCamera('Camara004', 'CamaraDir004', 0.001, function() {}, 0);
            });
            registerSetTimeout(1.3, function() {
              setHTMLElemStyle('display', 'none', 'fadeContainer', false);
              playSound(loadMedia_HTML5('../audios/area-4000-parte-c.mp3'), false);
              registerSetTimeout(2, function() {
                tweenCamera('Camara003', 'CamaraDir003', 2, function() {}, 0);
                registerSetTimeout(2.5, function() {

                  Function('app', 'v3d', 'puzzles', 'VARS', 'PROC', ' document.querySelector("#fadeContainer").classList.toggle("fade",false)')(appInstance, v3d, PL, VARS, PROC);

                  setHTMLElemStyle('display', 'flex', 'fadeContainer', false);
                  registerSetTimeout(1, function() {

                    Function('app', 'v3d', 'puzzles', 'VARS', 'PROC', '   document.querySelector("#fadeContainer").classList.toggle("fade")')(appInstance, v3d, PL, VARS, PROC);

                    registerSetTimeout(1, function() {
                      tweenCamera('Camara002', 'CamaraDir002', 0.001, function() {}, 0);
                    });
                    registerSetTimeout(1.3, function() {
                      setHTMLElemStyle('display', 'none', 'fadeContainer', false);
                      playSound(loadMedia_HTML5('../audios/area-4000-parte-d.mp3'), false);
                      registerSetTimeout(2, function() {
                        tweenCamera('Camara001', 'CamaraDir001', 2, function() {}, 0);
                      });
                    });
                  });
                });
              });
            });
          });
        });
        registerSetTimeout(2, function() {});
      });
      registerSetTimeout(21, function() {
        mostrarSubAreas();
        setHTMLElemStyle('display', 'none', 'invisibleContainer', false);
        setHTMLElemStyle('display', 'none', 'btn_mute_audio', false);
        setHTMLElemStyle('display', 'none', 'btn_unmute_audio', false);
        deshabilitar_boton_retornar();
      });
    });
  });
});

ocultar_objetos_riesgos_all();
setActiveCamera('cam_animada');
ocultarSubAreas();
tweenCamera('Camara006', 'CamaraDir006', 0.001, function() {}, 0);

registerSetTimeout(5, function() {});

registerOnHover('Icono001.004', false, function() {
  setHTMLElemStyle('cursor', 'pointer', ['BODY'], false);
}, function() {
  setHTMLElemStyle('cursor', 'auto', ['BODY'], false);
});
registerOnHover('Icono001.001', false, function() {
  setHTMLElemStyle('cursor', 'pointer', ['BODY'], false);
}, function() {
  setHTMLElemStyle('cursor', 'auto', ['BODY'], false);
});
registerOnHover('Icono001.002', false, function() {
  setHTMLElemStyle('cursor', 'pointer', ['BODY'], false);
}, function() {
  setHTMLElemStyle('cursor', 'auto', ['BODY'], false);
});
registerOnHover('Icono001.003', false, function() {
  setHTMLElemStyle('cursor', 'pointer', ['BODY'], false);
}, function() {
  setHTMLElemStyle('cursor', 'auto', ['BODY'], false);
});
registerOnHover('Icono001.005', false, function() {
  setHTMLElemStyle('cursor', 'pointer', ['BODY'], false);
}, function() {
  setHTMLElemStyle('cursor', 'auto', ['BODY'], false);
});
registerOnHover('Icono001.006', false, function() {
  setHTMLElemStyle('cursor', 'pointer', ['BODY'], false);
}, function() {
  setHTMLElemStyle('cursor', 'auto', ['BODY'], false);
});
registerOnHover('Icono001.007', false, function() {
  setHTMLElemStyle('cursor', 'pointer', ['BODY'], false);
}, function() {
  setHTMLElemStyle('cursor', 'auto', ['BODY'], false);
});
registerOnHover('Icono001.008', false, function() {
  setHTMLElemStyle('cursor', 'pointer', ['BODY'], false);
}, function() {
  setHTMLElemStyle('cursor', 'auto', ['BODY'], false);
});
registerOnHover('Icono001.009', false, function() {
  setHTMLElemStyle('cursor', 'pointer', ['BODY'], false);
}, function() {
  setHTMLElemStyle('cursor', 'auto', ['BODY'], false);
});

registerOnClick('i4120-BX-004', false, false, [0,1,2], function() {
  setInformacion('cajon-carga-hirociclones');
}, function() {});
registerOnClick('i4120-BX-004(1)', false, false, [0,1,2], function() {
  setInformacion('cajon-carga-hirociclones-1');
}, function() {});
registerOnClick('i4120-BX-005', false, false, [0,1,2], function() {
  setInformacion('cajon-lamas-primera-etapa');
}, function() {});
registerOnClick('i4120-BX-006', false, false, [0,1,2], function() {
  setInformacion('cajon-arena-primera-etapa');
}, function() {});
registerOnClick('i4120-BX-012', false, false, [0,1,2], function() {
  setInformacion('cajon-lamas-segunda-etapa');
}, function() {});
registerOnClick('i4120-CY-001/002', false, false, [0,1,2], function() {
  setInformacion('nido-hidrociclones-primera-etapa');
}, function() {});
registerOnClick('i4120-CY-001/002.001', false, false, [0,1,2], function() {
  setInformacion('nido-hidrociclones-primera-etapa-2');
}, function() {});
registerOnClick('i4120-CY-003/004', false, false, [0,1,2], function() {
  setInformacion('nido-hidrociclones-segunda-etapa');
}, function() {});
registerOnClick('i4120-CY-003/004.001', false, false, [0,1,2], function() {
  setInformacion('nido-hidrociclones-segunda-etapa-2');
}, function() {});
registerOnClick('i4320-PU-021@031', false, false, [0,1,2], function() {
  setInformacion('bombas-agua-recuperada-captacion-torre-1');
}, function() {});
registerOnClick('i4320-PU-021@031(1)', false, false, [0,1,2], function() {
  setInformacion('bombas-agua-recuperada-captacion-torre-2');
}, function() {});
registerOnClick('i4320-PU-021@031(2)', false, false, [0,1,2], function() {
  setInformacion('bombas-agua-recuperada-captacion-torre-3');
}, function() {});
registerOnClick('i4330-PU-@011', false, false, [0,1,2], function() {
  setInformacion('bombas-impulsion-agua-recuperada-1');
}, function() {});
registerOnClick('i4330-PU-@012', false, false, [0,1,2], function() {
  setInformacion('bombas-impulsion-agua-recuperada-2');
}, function() {});
registerOnClick('i4330-PU-@013', false, false, [0,1,2], function() {
  setInformacion('bombas-impulsion-agua-recuperada-3');
}, function() {});
registerOnClick('i4330-PU-@014', false, false, [0,1,2], function() {
  setInformacion('bombas-impulsion-agua-recuperada-4');
}, function() {});
registerOnClick('i4330-PU-@015', false, false, [0,1,2], function() {
  setInformacion('bombas-impulsion-agua-recuperada-5');
}, function() {});
registerOnClick('i4330-PU-@016', false, false, [0,1,2], function() {
  setInformacion('bombas-impulsion-agua-recuperada-6');
}, function() {});
registerOnClick('i4330-TK-005', false, false, [0,1,2], function() {
  setInformacion('tanque-estacion-intermedia');
}, function() {});

eventHTMLElem('click', 'go-back', true, function(event) {
  goBack();
  ocultar_objetos_riesgos_all();
});
eventHTMLElem('click', 'go-area', true, function(event) {
  goBack();
  ocultar_objetos_riesgos_all();
});
eventHTMLElem('click', 'cam-torre-captacion-1', true, function(event) {
  setUserPosition('CamaraDireccionTorre1', false, 'CamaraTorre1', 'Icono001.004');
});
eventHTMLElem('click', 'cam-torre-captacion-2', true, function(event) {
  setUserPosition('CamaraDireccionTorre2', false, 'CamaraTorre2', 'Icono001.001');
});
eventHTMLElem('click', 'cam-torre-captacion-3', true, function(event) {
  setUserPosition('CamaraDireccionTorre3', false, 'CamaraTorre3', 'Icono001.002');
});
eventHTMLElem('click', 'cam-carga-hidrociclon', true, function(event) {
  setUserPosition('CamaraDireccionCajon', false, 'CamaraCajon', 'Icono001.003');
});
eventHTMLElem('click', 'cam-nidos-hidrociclones-primera-etapa', true, function(event) {
  setUserPosition('CamaraDireccionHidrociclones', false, 'CamaraHidrociclones', 'Icono001.005');
});
eventHTMLElem('click', 'cam-cajon-descarga-hidrociclones', true, function(event) {
  setUserPosition('CamaraDireccionCajonLamas1', false, 'CamaraCajonLamas1', 'Icono001.006');
});
eventHTMLElem('click', 'cam-nidos-hidrociclones-segunda-etapa', true, function(event) {
  setUserPosition('CamaraDireccionHidro2', false, 'CamaraHidro2', 'Icono001.007');
});
eventHTMLElem('click', 'cam-cajones-descarga-segunda-etapa', true, function(event) {
  setUserPosition('CamaraDireccionCajon3', false, 'CamaraCajon3', 'Icono001.008');
});
eventHTMLElem('click', 'cam-estacion-intermedia', true, function(event) {
  setUserPosition('CamaraDireccionEstacion', false, 'CamaraEstacion', 'Icono001.009');
});

registerOnClick('u-1 torre de captacion 1 ', false, false, [0,1,2], function() {
  setUserPosition('cam_u-1 torre de captacion 1 ', 'operador-torre-captacion-1', 'u-1 torre de captacion 1 ', null);
}, function() {});
registerOnClick('u-2 torre de captacion 2', false, false, [0,1,2], function() {
  setUserPosition('cam_u-2 torre de captacion 2', 'operador-torre-captacion-2', 'u-2 torre de captacion 2', null);
}, function() {});
registerOnClick('u-3 torre de captacion 3 ', false, false, [0,1,2], function() {
  setUserPosition('cam_u-3 torre de captacion 3 ', 'operador-torre-captacion-3', 'u-3 torre de captacion 3 ', null);
}, function() {});
registerOnClick('u-4 cajon de carga de hidrociclon', false, false, [0,1,2], function() {
  setUserPosition('cam_u-4 cajon de carga de hidrociclon', 'operador-carga-hidrociclon', 'u-4 cajon de carga de hidrociclon', null);
}, function() {});
registerOnClick('u-5 Nidos de Hidrociclones Primera Etapa de Clasificación', false, false, [0,1,2], function() {
  setUserPosition('cam_u-5 Nidos de Hidrociclones Primera Etapa de Clasificación', 'operador-nidos-hidrociclones-primera-etapa', 'u-5 Nidos de Hidrociclones Primera Etapa de Clasificación', null);
}, function() {});
registerOnClick('u-6 cajon descarga de hidrociclones1', false, false, [0,1,2], function() {
  setUserPosition('cam_u-6 cajon descarga de hidrociclones1', 'operador-cajon-descarga-hidrociclones', 'u-6 cajon descarga de hidrociclones1', null);
}, function() {});
registerOnClick('u-7  Nidos de Hidrociclones Segunda Etapa de Clasificación', false, false, [0,1,2], function() {
  setUserPosition('cam_u-7  Nidos de Hidrociclones Segunda Etapa de Clasificación', 'operador-nidos-hidrociclones-segunda-etapa', 'u-7  Nidos de Hidrociclones Segunda Etapa de Clasificación', null);
}, function() {});
registerOnClick('u-8 cajones de descargas segunda etapa', false, false, [0,1,2], function() {
  setUserPosition('cam_u-8 cajones de descargas segunda etapa', 'operador-cajones-descarga-segunda-etapa', 'u-8 cajones de descargas segunda etapa', null);
}, function() {});
registerOnClick('u-9 estacion intermedia', false, false, [0,1,2], function() {
  setUserPosition('cam_u-9_estacion intermedia', 'operador-estacion-intermedia', 'u-9 estacion intermedia', null);
}, function() {});

operadores_visibles = ['u-1 torre de captacion 1 ', 'u-2 torre de captacion 2', 'u-3 torre de captacion 3 ', 'u-4 cajon de carga de hidrociclon', 'u-5 Nidos de Hidrociclones Primera Etapa de Clasificación', 'u-6 cajon descarga de hidrociclones1', 'u-7  Nidos de Hidrociclones Segunda Etapa de Clasificación', 'u-8 cajones de descargas segunda etapa', 'u-9 estacion intermedia'];



} // end of PL.init function

PL.disposeListeners = function() {
    if (_pGlob) {
        _pGlob.eventListeners.forEach(({ target, type, listener, optionsOrUseCapture }) => {
            target.removeEventListener(type, listener, optionsOrUseCapture);
        });
        _pGlob.eventListeners.length = 0;
    }
}

PL.dispose = function() {
    PL.disposeListeners();
    _pGlob = null;
    // backward compatibility
    if (v3d[Symbol.toStringTag] !== 'Module') {
        delete v3d.PL;
        delete v3d.puzzles;
    }
}



})(); // end of closure
